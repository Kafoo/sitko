let getters = {
     posts: state => {
         return state.posts
     },
     users: state => {
         return state.users
     },
     categories: state => {
         return state.categories
     }
}

export default  getters